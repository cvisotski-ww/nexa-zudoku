export declare const NO_IDENTITY = "__none";
export type Header = {
    name: string;
    defaultValue?: string;
};
export type QueryParam = {
    name: string;
    defaultValue?: string;
    defaultActive?: boolean;
    isRequired?: boolean;
};
export type PathParam = {
    name: string;
    defaultValue?: string;
    isRequired?: boolean;
};
export type PlaygroundForm = {
    body: string;
    queryParams: Array<{
        name: string;
        value: string;
        active: boolean;
    }>;
    pathParams: Array<{
        name: string;
        value: string;
    }>;
    headers: Array<{
        name: string;
        value: string;
    }>;
    identity?: string;
};
export type PlaygroundContentProps = {
    server: string;
    servers?: string[];
    url: string;
    method: string;
    headers?: Header[];
    queryParams?: QueryParam[];
    pathParams?: PathParam[];
    defaultBody?: string;
};
export declare const Playground: ({ server, servers, url, method, headers, queryParams, pathParams, defaultBody, }: PlaygroundContentProps) => import("react/jsx-runtime").JSX.Element;
export default Playground;
