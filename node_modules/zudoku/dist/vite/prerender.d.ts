export declare class FileWritingResponse {
    private readonly fileName;
    private buffer;
    private dontSave;
    private resolve;
    private resolved;
    set(): void;
    status(status: number): void;
    on(): void;
    constructor(fileName: string);
    redirect(): void;
    send: (chunk: string) => Promise<void>;
    write(chunk: string, _encoding?: string): void;
    end(chunk?: string): Promise<void>;
    isSent(): Promise<void>;
}
export declare const prerender: (html: string, dir: string) => Promise<string[]>;
