import { type Feedback } from '../../../hooks/useMessageFeedback';
import { FormConfig } from '../../Form/types';
import { type AIAnnotations } from './SubscribeToNewMessageForNewChat';
interface ChatActionsProps {
    ind: number;
    isHidden?: boolean;
    setShowFeedbackModal?: (ind: number) => void;
    submitPositiveFeedback?: (ind: number) => void;
    feedback: Feedback;
    handleCopyMessage: () => void;
    copyableMessage: string;
    openForm?: (form: FormConfig) => void;
    aiAnnotations: AIAnnotations;
    onModalClose?: () => void;
}
declare function ChatActions({ ind, isHidden, setShowFeedbackModal, submitPositiveFeedback, feedback, handleCopyMessage, copyableMessage, openForm, aiAnnotations, onModalClose, }: ChatActionsProps): import("react/jsx-runtime").JSX.Element;
export default ChatActions;
